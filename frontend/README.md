# Memory Map – Frontend

Next.js App Router UI. Install dependencies and run from this directory:

- `npm install`
- `npm run dev`

The app expects `NEXT_PUBLIC_API_BASE_URL` to point to the backend (default `http://localhost:4000`). Update `.env` in the repo root before starting.
